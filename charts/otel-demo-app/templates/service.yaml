apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo-app.fullname" . }}
  labels:
    {{- include "otel-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otel-demo-app.selectorLabels" . | nindent 4 }}
