apiVersion: v2
name: frontend-app
description: Mini e-commerce frontend instrumented with Grafana Faro (RUM + full-stack browser->backend tracing)
type: application
version: 0.1.0
appVersion: "1.0.0"
